# CSS 架構說明

## 📁 檔案結構

```
css/
├── styles.css           # 主要自訂樣式
├── tailwind.config.js   # Tailwind CSS 配置
└── README.md           # 本文檔
```

## 🎨 設計系統

### CSS 變數 (Design Tokens)

所有顏色、間距、圓角等設計元素都使用 CSS 變數管理，位於 `styles.css` 的 `:root` 選擇器中。

#### 色彩系統

```css
/* 主題色 */
--color-corp-600: #0284c7;  /* 主要按鈕、強調元素 */
--color-corp-800: #075985;  /* 深色變體 */

/* 中性色 */
--color-slate-xxx: ...      /* 文字、背景、邊框 */

/* 功能色 */
--color-emerald-xxx: ...    /* 成功、確認 */
--color-indigo-xxx: ...     /* 資訊、次要動作 */
--color-orange-xxx: ...     /* 警告 */
--color-rose-xxx: ...       /* 錯誤、刪除 */
```

#### 間距系統

```css
--spacing-xs: 0.25rem;   /* 4px  - 極小間距 */
--spacing-sm: 0.5rem;    /* 8px  - 小間距 */
--spacing-md: 1rem;      /* 16px - 標準間距 */
--spacing-lg: 1.5rem;    /* 24px - 大間距 */
--spacing-xl: 2rem;      /* 32px - 超大間距 */
```

#### 動畫時間

```css
--transition-fast: 150ms ease-in-out;  /* 快速互動 */
--transition-base: 200ms ease-in-out;  /* 標準動畫 */
--transition-slow: 300ms ease-out;     /* 平滑過渡 */
```

## 🧩 組件樣式

### 按鈕組件

- `.header-tab-btn` - 頁面標籤按鈕
- `.filter-btn` - 篩選器按鈕

### 互動組件

- `.accordion-content` - 手風琴內容區
- `.accordion-icon` - 手風琴圖示
- `.modal-overlay` - Modal 遮罩層

### 狀態樣式

- `.active-date` - 日期選擇器活躍狀態
- `.highlight-card` - 錨點跳轉高亮效果

### 工具類別

- `.custom-scroll` - 自訂捲軸樣式
- `.no-scrollbar` - 隱藏捲軸
- `.text-truncate` - 單行文字截斷
- `.line-clamp-2/3` - 多行文字截斷

## 🚀 使用方式

### 使用 CSS 變數

```css
/* 在自訂樣式中使用變數 */
.my-element {
  color: var(--color-corp-600);
  padding: var(--spacing-md);
  border-radius: var(--radius-lg);
  transition: all var(--transition-base);
}
```

### 擴展顏色

如需新增顏色，請在 `:root` 中定義：

```css
:root {
  --color-custom-500: #yourcolor;
}
```

## 📊 優化效益

相較於內嵌樣式，此架構提供：

- ✅ **更好的快取** - CSS 檔案可被瀏覽器快取
- ✅ **易於維護** - 集中管理所有樣式
- ✅ **主題系統** - CSS 變數支援動態主題切換
- ✅ **減少重複** - 統一的設計 token
- ✅ **更快載入** - HTML 檔案體積減少 ~60 行

## 🔮 未來擴展

### 深色模式

已預留深色模式支援架構：

```css
@media (prefers-color-scheme: dark) {
  :root {
    /* 重新定義變數值即可 */
  }
}
```

### 列印樣式

已包含基礎列印樣式，使用 `.no-print` 類別隱藏不需列印的元素。

## 📝 注意事項

1. **Tailwind 優先** - 優先使用 Tailwind utility classes
2. **變數命名** - 遵循 `--color-{name}-{shade}` 格式
3. **瀏覽器支援** - CSS 變數需 IE11 以上版本
4. **效能優化** - 使用 `.gpu-accelerate` 類別啟用 GPU 加速

## 🛠️ 維護指南

### 新增組件樣式

1. 在 `styles.css` 對應區塊新增
2. 使用 CSS 變數而非硬編碼值
3. 添加適當的註解說明用途

### 修改主題色

只需修改 `:root` 中的變數值，所有使用該變數的元素會自動更新。
