# 貢獻指南 (Contributing Guide)

首先，感謝您考慮為 Daily Report System 做出貢獻！正是像您這樣的人讓開源社群如此美好。

## 📋 目錄

- [行為準則](#行為準則)
- [如何貢獻](#如何貢獻)
- [開發流程](#開發流程)
- [代碼規範](#代碼規範)
- [提交訊息規範](#提交訊息規範)
- [Pull Request 流程](#pull-request-流程)

---

## 行為準則

### 我們的承諾

為了營造一個開放和友善的環境，我們承諾讓參與項目和社群的每個人都不受騷擾，無論其年齡、體型、殘疾、族裔、性別認同和表達、經驗水平、國籍、個人外貌、種族、宗教或性取向如何。

### 我們的標準

有助於創造積極環境的行為包括：

- 使用友善和包容的語言
- 尊重不同的觀點和經驗
- 優雅地接受建設性批評
- 關注對社群最有利的事情
- 對其他社群成員表示同理心

### 執行

您可以通過 [project email] 向項目團隊報告不當行為。

---

## 如何貢獻

### 🐛 報告錯誤 (Bug Reports)

如果您發現錯誤，請：

1. **檢查是否已有相關 Issue**
2. **創建新的 Issue**，包含：
   - 清晰的標題
   - 詳細的描述
   - 重現步驟
   - 預期行為
   - 實際行為
   - 截圖（如適用）
   - 環境資訊（瀏覽器、版本等）

**Issue 模板範例：**

```markdown
**描述錯誤**
當點擊「更新」按鈕時，系統沒有反應。

**重現步驟**
1. 進入「資料來源」頁面
2. 輸入 Google Sheet ID
3. 點擊「讀取資料」按鈕
4. 發現沒有任何反應

**預期行為**
應該顯示載入動畫並開始讀取資料。

**截圖**
[附上截圖]

**環境**
- 瀏覽器: Chrome 120
- 作業系統: macOS 14.0
- 版本: 2.0.0
```

### ✨ 功能請求 (Feature Requests)

我們歡迎新功能建議！請：

1. **描述功能** - 清楚說明您想要什麼
2. **解釋原因** - 為什麼這個功能有用
3. **提供範例** - 如果可能，提供使用範例

### 📝 文檔改進

文檔改進總是受歡迎的：

- 修正拼寫或語法錯誤
- 添加範例或教程
- 改善解釋的清晰度
- 翻譯文檔

---

## 開發流程

### 1. Fork 倉庫

點擊 GitHub 上的 "Fork" 按鈕。

### 2. 克隆您的 Fork

```bash
git clone https://github.com/YOUR_USERNAME/daily-report-system.git
cd daily-report-system
```

### 3. 添加上游倉庫

```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/daily-report-system.git
```

### 4. 創建功能分支

```bash
git checkout -b feature/your-feature-name
```

**分支命名規範：**
- `feature/功能名稱` - 新功能
- `fix/問題描述` - 錯誤修復
- `docs/文檔主題` - 文檔更新
- `refactor/重構描述` - 代碼重構
- `test/測試描述` - 測試相關

### 5. 設置開發環境

```bash
# 啟動本地伺服器（選擇其一）
python3 -m http.server 8000
# 或
npx http-server -p 8000
```

### 6. 進行更改

請確保：
- 遵循代碼規範
- 添加適當的註解
- 更新相關文檔
- 測試您的更改

### 7. 提交更改

```bash
git add .
git commit -m "feat: 添加新功能描述"
```

### 8. 保持同步

```bash
git fetch upstream
git rebase upstream/main
```

### 9. 推送到您的 Fork

```bash
git push origin feature/your-feature-name
```

### 10. 創建 Pull Request

前往 GitHub 並創建 Pull Request。

---

## 代碼規範

### JavaScript

#### 命名規範

```javascript
// 變數和函數 - camelCase
const userName = 'John';
function getUserData() { }

// 常量 - UPPER_SNAKE_CASE
const MAX_RETRIES = 3;
const API_ENDPOINT = 'https://api.example.com';

// 類別 - PascalCase
class UserManager { }

// 私有變數/函數 - 前綴下劃線
let _privateVar = 'private';
function _privateFunction() { }
```

#### 函數規範

```javascript
/**
 * 函數應該有清晰的 JSDoc 註解
 * @param {string} userId - 用戶 ID
 * @param {Object} options - 選項對象
 * @param {boolean} options.includeDetails - 是否包含詳細資訊
 * @returns {Promise<User>} 用戶對象
 */
async function fetchUser(userId, options = {}) {
    // 實作
}
```

#### ES6+ 特性

優先使用：
- `const` 和 `let`（不使用 `var`）
- 箭頭函數
- 模板字串
- 解構賦值
- 展開運算符
- 可選鏈 (`?.`)
- 空值合併 (`??`)

```javascript
// ✅ 好的做法
const user = users.find(u => u.id === userId);
const { name, email } = user ?? {};
const greeting = `Hello, ${name}`;

// ❌ 避免
var user = users.find(function(u) {
    return u.id === userId;
});
var name = user && user.name;
var greeting = 'Hello, ' + name;
```

### CSS

#### 使用 Tailwind Utility Classes

優先使用 Tailwind，必要時才自訂 CSS：

```html
<!-- ✅ 優先使用 Tailwind -->
<div class="flex items-center justify-between p-4 bg-white rounded-lg">

<!-- ⚠️ 必要時使用自訂類別 -->
<div class="custom-complex-component">
```

#### 自訂 CSS 規範

```css
/* 使用 CSS 變數 */
.custom-button {
    background-color: var(--color-corp-600);
    padding: var(--spacing-md);
    border-radius: var(--radius-lg);
}

/* BEM 命名（如需要） */
.card {}
.card__header {}
.card__body {}
.card--large {}
```

### HTML

```html
<!-- 使用語義化標籤 -->
<header>...</header>
<nav>...</nav>
<main>...</main>
<article>...</article>
<footer>...</footer>

<!-- 可訪問性 -->
<button aria-label="關閉對話框">
<img src="..." alt="描述性替代文字">
<input type="text" id="name" aria-describedby="name-help">
```

---

## 提交訊息規範

我們遵循 [Conventional Commits](https://www.conventionalcommits.org/) 規範。

### 格式

```
<類型>(<範圍>): <簡短描述>

<詳細描述>

<頁尾>
```

### 類型

- `feat`: 新功能
- `fix`: 錯誤修復
- `docs`: 文檔變更
- `style`: 代碼格式（不影響功能）
- `refactor`: 重構
- `perf`: 性能改進
- `test`: 測試相關
- `chore`: 建置過程或輔助工具變更

### 範例

```
feat(charts): 添加趨勢圖表互動功能

- 添加點擊事件處理
- 實作數據篩選邏輯
- 更新圖表樣式

Closes #123
```

```
fix(api): 修復 Google Sheets API 重試邏輯

當 API 請求失敗時，重試機制沒有正確觸發。
現在會正確執行指數退避重試。

Fixes #456
```

---

## Pull Request 流程

### PR 檢查清單

在提交 PR 前，請確認：

- [ ] 代碼遵循項目的代碼規範
- [ ] 添加了適當的 JSDoc 註解
- [ ] 更新了相關文檔
- [ ] 測試了所有更改
- [ ] 提交訊息符合規範
- [ ] PR 描述清晰完整

### PR 描述模板

```markdown
## 變更描述
簡要描述此 PR 的目的。

## 變更類型
- [ ] 錯誤修復
- [ ] 新功能
- [ ] 文檔更新
- [ ] 代碼重構
- [ ] 其他 (請說明)

## 相關 Issue
Closes #123

## 測試方式
描述如何測試此變更：
1. 步驟一
2. 步驟二
3. 預期結果

## 截圖（如適用）
[附上截圖]

## 檢查清單
- [ ] 代碼遵循規範
- [ ] 添加了註解
- [ ] 更新了文檔
- [ ] 已測試變更
```

### Review 流程

1. **自動檢查** - GitHub Actions 會運行自動檢查
2. **代碼審查** - 維護者會審查您的代碼
3. **討論** - 可能會有討論和建議
4. **修改** - 根據反饋進行必要的修改
5. **合併** - 通過審查後會被合併

---

## 開發技巧

### 本地測試

```bash
# 在不同瀏覽器測試
- Chrome
- Firefox
- Safari
- Edge

# 測試響應式設計
- 桌面 (1920x1080)
- 平板 (768x1024)
- 手機 (375x667)
```

### 除錯工具

```javascript
// 使用開發者模式
if (window.__DEBUG__) {
    console.log('Debug info:', data);
}

// 使用瀏覽器開發者工具
// - Console
// - Network
// - Application (查看 LocalStorage)
// - Performance
```

### 有用的資源

- [MDN Web Docs](https://developer.mozilla.org/)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [Chart.js Docs](https://www.chartjs.org/docs/latest/)
- [ES6 Features](https://github.com/lukehoban/es6features)

---

## 問題？

如有任何問題，請：

1. 查看 [README.md](../README.md)
2. 搜尋現有 [Issues](https://github.com/yourusername/daily-report-system/issues)
3. 加入我們的 [Discussions](https://github.com/yourusername/daily-report-system/discussions)
4. 聯繫維護者

---

## 致謝

感謝所有貢獻者！您的幫助讓這個項目變得更好。

[所有貢獻者列表](https://github.com/yourusername/daily-report-system/graphs/contributors)

---

**Happy Coding! 🚀**
